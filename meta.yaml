# cmd conda build . -c conda-forge
package:
    name: onedmin
    version: "1.0.0"

source:
    path: .

requirements:
    build:
        - cmake
        - gfortran_linux-64  # [linux]
        - blas
        - openblas # [linux]
        - lapack
    run:
        - cmake
        - gfortran_linux-64  # [linux]
        - blas
        - openblas # [linux]
        - lapack
